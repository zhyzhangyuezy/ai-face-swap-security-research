# Manifest Directory

Full CSV manifests can be large and may contain local path identifiers, so they
are ignored by Git by default. Public releases should either:

- regenerate them from the scripts in `prototype/scripts/`, or
- publish the exact paper-facing manifests in an external artifact bundle when
  dataset licenses permit it.

The paper-facing manifest names are listed in the supplementary material and in
`DATA.md`.
