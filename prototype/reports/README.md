# Report Directory

This directory is for lightweight paper-facing summaries such as `.tsv`, `.json`,
and `.md` files. Dense per-row `.csv` score dumps are ignored by Git because they
can be large and often contain local path identifiers.

If a reviewer or collaborator needs exact dense score dumps, publish them as a
separate artifact bundle and record checksums in `DATA.md` or a release note.
